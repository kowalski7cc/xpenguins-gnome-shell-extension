Check if '.exists' is necessary: I think it's because xpenguins allocates *space* for every type and only says it 'exists' if there is one.
I don't have to allocate memory so I can just do ` if (ToonData[this.genus]['type']) ` instead of `if (ToonData[this.genus]['type'].exists)`
